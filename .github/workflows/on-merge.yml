name: Do something after merge

on:
  push:
    branches:
      - master
    paths:
      - "**.txt"

jobs:
  pushed:
    runs-on: ubuntu-latest
    outputs:
      modified_matrix: ${{ steps.create_matrix.outputs.modified_matrix }}
      deleted_matrix: ${{ steps.create_matrix.outputs.deleted_matrix }}
      any_changed: ${{ steps.files.outputs.any_changed }}
      any_deleted: ${{ steps.files.outputs.any_deleted }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          # With the default value of 1, there are corner cases where tj-actions/changed-files
          # fails with a `no merge base` error
          fetch-depth: 0

      - name: Get changed files
        id: files
        uses: tj-actions/changed-files@v34
        with:
          json: true
          include_all_old_new_renamed_files: true
          files: |
            **/*.txt

      - name: Create matrixes
        id: create_matrix
        run: |
          echo "modified_matrix={\"file\": ${{ steps.files.outputs.all_changed_files }}}" >> $GITHUB_OUTPUT
          echo "deleted_matrix={\"file\": ${{ steps.files.outputs.deleted_files }}}" >> $GITHUB_OUTPUT

  modified:
    needs: pushed
    if: needs.pushed.outputs.any_changed
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.pushed.outputs.modified_matrix) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Print file
        run: |
          echo "Printing file ${{ matrix.file }}"
          cat ${{ matrix.file }}

  deleted:
    needs: pushed
    if: needs.pushed.outputs.any_deleted
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.pushed.outputs.deleted_matrix) }}

    steps:
      - name: Print file
        run: |
          echo "File ${{ matrix.file }} has been deleted"
