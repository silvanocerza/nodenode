pretext: Triggered via {{eventName}} by {{actor}} {{or action "action"}} {{env.VERSION}}
title: "Project {{env.VERSION}} release"
title_link: "{{repositoryUrl}}/{{env.VERSION}}"

text: >
  {{icon jobStatus}}
  {{#if (eq jobStatus "SUCCESS")}}
    Project {{env.VERSION}} has been released :rocket:
  {{else if (eq jobStatus "FAILURE")}}
    Project {{env.VERSION}} release failed!
  {{else if (eq jobStatus "CANCELLED")}}
    Project {{env.VERSION}} release has been cancelled!
  {{/if}}

fallback: |-
  [GitHub] {{repositoryName}} {{env.VERSION}} release is {{jobStatus}}

footer: >-
  <{{repositoryUrl}}|{{repositoryName}}> <{{workflowRunUrl}}|{{workflow}} #{{runNumber}}>

colors:
  success: "#5DADE2"
  failure: "#884EA0"
  cancelled: "#A569BD"
  default: "#7D3C98"

icons:
  success: ":white_check_mark:"
  failure: ":x:"
  cancelled: ":interrobang:"
  skipped: ":interrobang:"
  default: ":interrobang:"
